---
-
  general_overview:
    objective: "Node compromise to get readonly kubelet API access."
    starting-point: "SSH node - any worker."
    num-hints: "6"
-
  hints:
    hint-1: "Task 1: Have you looked into the the kubelet?"
    hint-2: "Task 1: The kubelet readonly API sits on port 10255."
    hint-3: "Task 1: Post anything directly to the kubelet API, circumventing the Kubernetes server API."
    hint-4: "Task 1: You need to fix /var/lib/kubelet/config.yaml."
    hint-5: "Task 1: readOnly should be 0."
    hint-6: "Task 1: 'systemctl daemon-reload; systemctl restart kubelet.service' applies the config changes."
