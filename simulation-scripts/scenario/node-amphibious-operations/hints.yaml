---
-
  general_overview:
    objective: "Node compromise to get unauth kubelet API access"
    starting-point: "SSH node - any worker"
    num-hints: "6"
-
  hints:
    hint-1: "Have you looked into the the kubelet?"
    hint-2: "The kubelet read-write API sits on port 10250"
    hint-3: "Post anything to the kubelet API, circumventing the Kubernetes server API"
    hint-4: "You need to fix /var/lib/kubelet/config.yaml"
    hint-5: "authentication.anonymous.enabled value should be 'false' and authorization.mode should be 'Webhook'"
    hint-6: "'systemctl daemon-reload; systemctl restart kubelet.service' applies the config changes"
