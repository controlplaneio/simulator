--- 
- 
  general_overview: 
    detail: |
        Access mounted socket in nginx (used for reverse proxying for other containers).
        `docker inspect` the postgres container for passed env vars.
    task: "Get postgres password"
- 
  hints: 
    hint1: |
        What mounts do we have?
        Anything which might help expose other containers to us?
    hint2: "kubectl pod exec"
    hint3: "kubectl exec into workload"
    hint4: "workload is nginx-proxy with mounted docker.socket"
    hint5: "workload - enumerate r/o mounted docker.socket"
    hint6: "pull env vars from local docker inspect"
    hint7: "postgres containers password as env var"

