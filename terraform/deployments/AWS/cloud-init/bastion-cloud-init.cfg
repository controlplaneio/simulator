#cloud-config

package_update: true
package_upgrade: true
disable_root: false

runcmd:
  - 'apt install -y docker.io'
  - 'systemctl daemon-reload'
  - 'systemctl restart docker'
  - 'docker pull simulator-attack'

output:
  all: '| tee -a /var/log/cloud-init-output.log'

