---
port:
  tcp:22:
    listening: true
    ip:
      - 0.0.0.0

# Check sshd is running
service:
  sshd:
    enabled: true
    running: true

packages:
  kubectl:
    installed: true

command:
  cloud-init:
    exec: cloud-init status
    exit-status: 0
    stdout: ['status: done']
